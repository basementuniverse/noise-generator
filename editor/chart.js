/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
(function webpackUniversalModuleDefinition(root, factory) {
	if(typeof exports === 'object' && typeof module === 'object')
		module.exports = factory();
	else if(typeof define === 'function' && define.amd)
		define([], factory);
	else {
		var a = factory();
		for(var i in a) (typeof exports === 'object' ? exports : root)[i] = a[i];
	}
})(self, () => {
return /******/ (() => { // webpackBootstrap
/******/ 	"use strict";
/******/ 	var __webpack_modules__ = ({

/***/ "./index.ts"
/*!******************!*\
  !*** ./index.ts ***!
  \******************/
(__unused_webpack_module, exports) {

eval("{\nObject.defineProperty(exports, \"__esModule\", ({ value: true }));\nexports.drawChart = void 0;\nconst times = (f, n) => Array(n)\n    .fill(0)\n    .map((_, i) => f(i));\nconst range = (n) => times((i) => i, n);\nconst clamp = (a, min = 0, max = 1) => a < min ? min : a > max ? max : a;\nconst remap = (i, a1, a2, b1, b2) => b1 + ((i - a1) * (b2 - b1)) / (a2 - a1);\nconst round = (n, d = 0) => {\n    const p = Math.pow(10, d);\n    return Math.round(n * p + Number.EPSILON) / p;\n};\nconst DEFAULT_COLORS = [\n    '#FA6868',\n    '#71BEA0',\n    '#5A9CB5',\n    '#FAAC68',\n    '#609B8F',\n    '#2C4E80',\n    '#FACE68',\n    '#624E88',\n];\nconst DEFAULT_CHART_OPTIONS = {\n    position: { x: 0, y: 0 },\n    size: null,\n    padding: { top: 24, right: 16, bottom: 32, left: 44 },\n    pixelRatio: 1,\n    background: null,\n    type: 'line',\n    colors: DEFAULT_COLORS,\n    series: [],\n    xAxis: {\n        show: true,\n        grid: false,\n        ticks: 6,\n        range: {\n            padding: 0.02,\n            nice: false,\n            includeZero: false,\n        },\n    },\n    yAxis: {\n        show: true,\n        grid: true,\n        ticks: 6,\n        range: {\n            padding: 0.05,\n            nice: true,\n            includeZero: false,\n        },\n    },\n    axisStyle: {\n        axisColor: '#6b7280',\n        axisWidth: 1,\n        gridColor: '#e5e7eb',\n        gridWidth: 1,\n        tickColor: '#9ca3af',\n        tickWidth: 1,\n        tickLength: 4,\n        labelColor: '#4b5563',\n        labelFont: '12px sans-serif',\n        labelOffset: 8,\n    },\n};\nfunction isFiniteNumber(value) {\n    return typeof value === 'number' && Number.isFinite(value);\n}\nfunction isDataPoint(value) {\n    return !!value && typeof value === 'object' && 'x' in value && 'y' in value;\n}\nfunction toEdgeInsets(value) {\n    var _a, _b, _c, _d;\n    if (typeof value === 'number') {\n        return {\n            top: value,\n            right: value,\n            bottom: value,\n            left: value,\n        };\n    }\n    return {\n        top: (_a = value.top) !== null && _a !== void 0 ? _a : 0,\n        right: (_b = value.right) !== null && _b !== void 0 ? _b : 0,\n        bottom: (_c = value.bottom) !== null && _c !== void 0 ? _c : 0,\n        left: (_d = value.left) !== null && _d !== void 0 ? _d : 0,\n    };\n}\nfunction alphaColor(hexColor, alpha) {\n    const safeAlpha = clamp(alpha, 0, 1);\n    if (hexColor.startsWith('#') &&\n        (hexColor.length === 7 || hexColor.length === 4)) {\n        const fullHex = hexColor.length === 4\n            ? `#${hexColor[1]}${hexColor[1]}${hexColor[2]}${hexColor[2]}${hexColor[3]}${hexColor[3]}`\n            : hexColor;\n        const r = Number.parseInt(fullHex.slice(1, 3), 16);\n        const g = Number.parseInt(fullHex.slice(3, 5), 16);\n        const b = Number.parseInt(fullHex.slice(5, 7), 16);\n        return `rgba(${r}, ${g}, ${b}, ${safeAlpha})`;\n    }\n    return hexColor;\n}\nfunction resolveOptions(options) {\n    var _a, _b, _c, _d, _e, _f, _g, _h;\n    const xAxisRange = {\n        ...DEFAULT_CHART_OPTIONS.xAxis.range,\n        ...((_b = (_a = options.xAxis) === null || _a === void 0 ? void 0 : _a.range) !== null && _b !== void 0 ? _b : {}),\n    };\n    const yAxisRange = {\n        ...DEFAULT_CHART_OPTIONS.yAxis.range,\n        ...((_d = (_c = options.yAxis) === null || _c === void 0 ? void 0 : _c.range) !== null && _d !== void 0 ? _d : {}),\n    };\n    return {\n        ...DEFAULT_CHART_OPTIONS,\n        ...options,\n        position: options.position\n            ? { x: options.position.x, y: options.position.y }\n            : {\n                x: DEFAULT_CHART_OPTIONS.position.x,\n                y: DEFAULT_CHART_OPTIONS.position.y,\n            },\n        size: options.size\n            ? { x: options.size.x, y: options.size.y }\n            : DEFAULT_CHART_OPTIONS.size,\n        colors: options.colors && options.colors.length > 0\n            ? options.colors\n            : DEFAULT_CHART_OPTIONS.colors,\n        xAxis: {\n            ...DEFAULT_CHART_OPTIONS.xAxis,\n            ...((_e = options.xAxis) !== null && _e !== void 0 ? _e : {}),\n            range: xAxisRange,\n        },\n        yAxis: {\n            ...DEFAULT_CHART_OPTIONS.yAxis,\n            ...((_f = options.yAxis) !== null && _f !== void 0 ? _f : {}),\n            range: yAxisRange,\n        },\n        axisStyle: {\n            ...DEFAULT_CHART_OPTIONS.axisStyle,\n            ...((_g = options.axisStyle) !== null && _g !== void 0 ? _g : {}),\n        },\n        series: (_h = options.series) !== null && _h !== void 0 ? _h : DEFAULT_CHART_OPTIONS.series,\n    };\n}\nfunction inferXScaleKind(series) {\n    let hasTime = false;\n    let hasCategory = false;\n    for (const item of series) {\n        if (!Array.isArray(item.data)) {\n            continue;\n        }\n        for (const point of item.data) {\n            if (!isDataPoint(point)) {\n                continue;\n            }\n            if (typeof point.x === 'string') {\n                hasCategory = true;\n            }\n            else if (point.x instanceof Date) {\n                hasTime = true;\n            }\n        }\n    }\n    if (hasCategory) {\n        return 'category';\n    }\n    if (hasTime) {\n        return 'time';\n    }\n    return 'linear';\n}\nfunction xToLabel(value) {\n    if (value instanceof Date) {\n        return value.toISOString();\n    }\n    return `${value}`;\n}\nfunction normalizeSeries(series, chartType, colors) {\n    const xScaleKind = inferXScaleKind(series);\n    const categoryIndex = new Map();\n    const categories = [];\n    const getCategoryValue = (rawX) => {\n        const label = xToLabel(rawX);\n        const existing = categoryIndex.get(label);\n        if (existing !== undefined) {\n            return existing;\n        }\n        const next = categories.length;\n        categoryIndex.set(label, next);\n        categories.push(label);\n        return next;\n    };\n    const normalized = series.map((item, index) => {\n        var _a, _b, _c, _d, _e, _f, _g, _h, _j;\n        const data = item.data.map((entry, i) => {\n            if (typeof entry === 'number') {\n                return {\n                    x: xScaleKind === 'category' ? getCategoryValue(i) : i,\n                    y: isFiniteNumber(entry) ? entry : null,\n                    rawX: i,\n                };\n            }\n            const rawX = entry.x;\n            let xValue = 0;\n            if (xScaleKind === 'category') {\n                xValue = getCategoryValue(rawX);\n            }\n            else if (xScaleKind === 'time') {\n                xValue = rawX instanceof Date ? rawX.getTime() : Number(rawX);\n            }\n            else {\n                xValue = Number(rawX);\n            }\n            return {\n                x: isFiniteNumber(xValue) ? xValue : i,\n                y: isFiniteNumber(entry.y) ? entry.y : null,\n                rawX,\n            };\n        });\n        const color = (_b = (_a = item.color) !== null && _a !== void 0 ? _a : colors[index % colors.length]) !== null && _b !== void 0 ? _b : '#1f77b4';\n        const opacity = (_c = item.opacity) !== null && _c !== void 0 ? _c : 1;\n        return {\n            name: (_d = item.name) !== null && _d !== void 0 ? _d : `Series ${index + 1}`,\n            type: (_e = item.type) !== null && _e !== void 0 ? _e : chartType,\n            data,\n            color,\n            fill: (_f = item.fill) !== null && _f !== void 0 ? _f : alphaColor(color, 0.2),\n            width: (_g = item.width) !== null && _g !== void 0 ? _g : 2,\n            pointRadius: (_h = item.pointRadius) !== null && _h !== void 0 ? _h : (item.type === 'scatter' ? 3 : 0),\n            opacity,\n            hidden: (_j = item.hidden) !== null && _j !== void 0 ? _j : false,\n        };\n    });\n    return {\n        xScaleKind,\n        categories,\n        series: normalized,\n    };\n}\nfunction toDomainValue(value) {\n    if (value === undefined) {\n        return undefined;\n    }\n    if (value instanceof Date) {\n        return value.getTime();\n    }\n    return Number(value);\n}\nfunction ensureSpan(domain) {\n    if (domain.min !== domain.max) {\n        return domain;\n    }\n    const delta = domain.min === 0 ? 1 : Math.abs(domain.min) * 0.1;\n    return {\n        min: domain.min - delta,\n        max: domain.max + delta,\n    };\n}\nfunction withPadding(domain, padding) {\n    if (padding <= 0) {\n        return domain;\n    }\n    const span = domain.max - domain.min;\n    const offset = span * padding;\n    return {\n        min: domain.min - offset,\n        max: domain.max + offset,\n    };\n}\nfunction niceStep(step) {\n    const exponent = Math.floor(Math.log10(step));\n    const fraction = step / 10 ** exponent;\n    let niceFraction = 1;\n    if (fraction >= 7.5) {\n        niceFraction = 10;\n    }\n    else if (fraction >= 3.5) {\n        niceFraction = 5;\n    }\n    else if (fraction >= 1.5) {\n        niceFraction = 2;\n    }\n    return niceFraction * 10 ** exponent;\n}\nfunction applyNice(domain, tickCount) {\n    const safeTickCount = Math.max(2, tickCount);\n    const span = domain.max - domain.min;\n    if (span <= 0 || !Number.isFinite(span)) {\n        return domain;\n    }\n    const step = niceStep(span / (safeTickCount - 1));\n    return {\n        min: Math.floor(domain.min / step) * step,\n        max: Math.ceil(domain.max / step) * step,\n    };\n}\nfunction computeDomains(series, xScaleKind, categories, xAxis, yAxis) {\n    var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m;\n    const visibleSeries = series.filter(item => !item.hidden);\n    const allX = visibleSeries\n        .flatMap(item => item.data.map(point => point.x))\n        .filter(value => Number.isFinite(value));\n    const allY = visibleSeries\n        .flatMap(item => item.data.map(point => point.y))\n        .filter(isFiniteNumber);\n    let xDomain;\n    if (xScaleKind === 'category') {\n        if (categories.length === 0) {\n            xDomain = { min: -0.5, max: 0.5 };\n        }\n        else {\n            xDomain = { min: -0.5, max: categories.length - 0.5 };\n        }\n    }\n    else {\n        const xMin = allX.length > 0 ? Math.min(...allX) : 0;\n        const xMax = allX.length > 0 ? Math.max(...allX) : 1;\n        xDomain = ensureSpan({ min: xMin, max: xMax });\n    }\n    const yMin = allY.length > 0 ? Math.min(...allY) : 0;\n    const yMax = allY.length > 0 ? Math.max(...allY) : 1;\n    let yDomain = ensureSpan({ min: yMin, max: yMax });\n    const hasBars = visibleSeries.some(item => item.type === 'bar');\n    const includeZero = (_b = (_a = yAxis.range) === null || _a === void 0 ? void 0 : _a.includeZero) !== null && _b !== void 0 ? _b : hasBars;\n    if (includeZero) {\n        yDomain = {\n            min: Math.min(0, yDomain.min),\n            max: Math.max(0, yDomain.max),\n        };\n    }\n    const xMinOverride = toDomainValue((_c = xAxis.range) === null || _c === void 0 ? void 0 : _c.min);\n    const xMaxOverride = toDomainValue((_d = xAxis.range) === null || _d === void 0 ? void 0 : _d.max);\n    const yMinOverride = toDomainValue((_e = yAxis.range) === null || _e === void 0 ? void 0 : _e.min);\n    const yMaxOverride = toDomainValue((_f = yAxis.range) === null || _f === void 0 ? void 0 : _f.max);\n    xDomain = {\n        min: Number.isFinite(xMinOverride) ? xMinOverride : xDomain.min,\n        max: Number.isFinite(xMaxOverride) ? xMaxOverride : xDomain.max,\n    };\n    yDomain = {\n        min: Number.isFinite(yMinOverride) ? yMinOverride : yDomain.min,\n        max: Number.isFinite(yMaxOverride) ? yMaxOverride : yDomain.max,\n    };\n    xDomain = ensureSpan(xDomain);\n    yDomain = ensureSpan(yDomain);\n    if (xScaleKind !== 'category') {\n        xDomain = withPadding(xDomain, (_h = (_g = xAxis.range) === null || _g === void 0 ? void 0 : _g.padding) !== null && _h !== void 0 ? _h : 0);\n    }\n    yDomain = withPadding(yDomain, (_k = (_j = yAxis.range) === null || _j === void 0 ? void 0 : _j.padding) !== null && _k !== void 0 ? _k : 0);\n    if ((_l = yAxis.range) === null || _l === void 0 ? void 0 : _l.nice) {\n        yDomain = applyNice(yDomain, (_m = yAxis.ticks) !== null && _m !== void 0 ? _m : 6);\n    }\n    return { xDomain, yDomain };\n}\nfunction buildPlotRect(context, options) {\n    var _a;\n    const canvasWidth = options.size\n        ? options.size.x\n        : Math.max(1, context.canvas.width - options.position.x);\n    const canvasHeight = options.size\n        ? options.size.y\n        : Math.max(1, context.canvas.height - options.position.y);\n    const padding = toEdgeInsets(options.padding);\n    const titleHeight = ((_a = options.title) === null || _a === void 0 ? void 0 : _a.text) ? 20 : 0;\n    const plotRect = {\n        x: options.position.x + padding.left,\n        y: options.position.y + padding.top + titleHeight,\n        width: Math.max(1, canvasWidth - padding.left - padding.right),\n        height: Math.max(1, canvasHeight - padding.top - padding.bottom - titleHeight),\n    };\n    return {\n        canvasWidth,\n        canvasHeight,\n        plotRect,\n    };\n}\nfunction makeScales(plotRect, xDomain, yDomain) {\n    const toCanvasX = (value) => remap(value, xDomain.min, xDomain.max, plotRect.x, plotRect.x + plotRect.width);\n    const toCanvasY = (value) => remap(value, yDomain.min, yDomain.max, plotRect.y + plotRect.height, plotRect.y);\n    const clampX = (value) => clamp(value, plotRect.x, plotRect.x + plotRect.width);\n    const clampY = (value) => clamp(value, plotRect.y, plotRect.y + plotRect.height);\n    return {\n        toCanvasX,\n        toCanvasY,\n        toClampedCanvasX: (value) => clampX(toCanvasX(value)),\n        toClampedCanvasY: (value) => clampY(toCanvasY(value)),\n    };\n}\nfunction makeTicks(domain, count) {\n    const safeCount = Math.max(2, count);\n    if (safeCount === 2) {\n        return [domain.min, domain.max];\n    }\n    const divisor = safeCount - 1;\n    return range(safeCount).map(index => remap(index, 0, divisor, domain.min, domain.max));\n}\nfunction sampleCategoryTicks(categories, maxCount) {\n    if (categories.length === 0) {\n        return [];\n    }\n    if (categories.length <= maxCount) {\n        return range(categories.length);\n    }\n    const step = Math.max(1, Math.ceil(categories.length / maxCount));\n    const ticks = [];\n    for (let i = 0; i < categories.length; i += step) {\n        ticks.push(i);\n    }\n    if (ticks[ticks.length - 1] !== categories.length - 1) {\n        ticks.push(categories.length - 1);\n    }\n    return ticks;\n}\nfunction defaultFormatNumber(value) {\n    return `${round(value, 2)}`;\n}\nfunction defaultFormatTime(value) {\n    return new Date(value).toLocaleDateString();\n}\nfunction drawAxes(context, plotRect, xScaleKind, categories, xDomain, yDomain, xAxis, yAxis, axisStyle, toCanvasX, toCanvasY) {\n    var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m, _o, _p;\n    const xTicks = xScaleKind === 'category'\n        ? sampleCategoryTicks(categories, (_a = xAxis.ticks) !== null && _a !== void 0 ? _a : 6)\n        : makeTicks(xDomain, (_b = xAxis.ticks) !== null && _b !== void 0 ? _b : 6);\n    const yTicks = makeTicks(yDomain, (_c = yAxis.ticks) !== null && _c !== void 0 ? _c : 6);\n    const axisColor = (_d = axisStyle.axisColor) !== null && _d !== void 0 ? _d : '#6b7280';\n    const axisWidth = (_e = axisStyle.axisWidth) !== null && _e !== void 0 ? _e : 1;\n    const gridColor = (_f = axisStyle.gridColor) !== null && _f !== void 0 ? _f : '#e5e7eb';\n    const gridWidth = (_g = axisStyle.gridWidth) !== null && _g !== void 0 ? _g : 1;\n    const tickColor = (_h = axisStyle.tickColor) !== null && _h !== void 0 ? _h : '#9ca3af';\n    const tickWidth = (_j = axisStyle.tickWidth) !== null && _j !== void 0 ? _j : 1;\n    const tickLength = (_k = axisStyle.tickLength) !== null && _k !== void 0 ? _k : 4;\n    const labelColor = (_l = axisStyle.labelColor) !== null && _l !== void 0 ? _l : '#4b5563';\n    const labelFont = (_m = axisStyle.labelFont) !== null && _m !== void 0 ? _m : '12px sans-serif';\n    const labelOffset = (_o = axisStyle.labelOffset) !== null && _o !== void 0 ? _o : 8;\n    context.save();\n    context.strokeStyle = tickColor;\n    context.lineWidth = tickWidth;\n    context.font = labelFont;\n    if (yAxis.grid) {\n        context.strokeStyle = gridColor;\n        context.lineWidth = gridWidth;\n        for (const value of yTicks) {\n            const y = toCanvasY(value);\n            context.beginPath();\n            context.moveTo(plotRect.x, y);\n            context.lineTo(plotRect.x + plotRect.width, y);\n            context.stroke();\n        }\n    }\n    if (xAxis.grid) {\n        context.strokeStyle = gridColor;\n        context.lineWidth = gridWidth;\n        for (const value of xTicks) {\n            const x = toCanvasX(value);\n            context.beginPath();\n            context.moveTo(x, plotRect.y);\n            context.lineTo(x, plotRect.y + plotRect.height);\n            context.stroke();\n        }\n    }\n    context.strokeStyle = axisColor;\n    context.lineWidth = axisWidth;\n    if (xAxis.show) {\n        context.beginPath();\n        context.moveTo(plotRect.x, plotRect.y + plotRect.height);\n        context.lineTo(plotRect.x + plotRect.width, plotRect.y + plotRect.height);\n        context.stroke();\n    }\n    if (yAxis.show) {\n        context.beginPath();\n        context.moveTo(plotRect.x, plotRect.y);\n        context.lineTo(plotRect.x, plotRect.y + plotRect.height);\n        context.stroke();\n    }\n    context.strokeStyle = tickColor;\n    context.lineWidth = tickWidth;\n    if (yAxis.show && tickLength > 0) {\n        for (const value of yTicks) {\n            const y = toCanvasY(value);\n            context.beginPath();\n            context.moveTo(plotRect.x, y);\n            context.lineTo(plotRect.x - tickLength, y);\n            context.stroke();\n        }\n    }\n    if (xAxis.show && tickLength > 0) {\n        const axisY = plotRect.y + plotRect.height;\n        for (const value of xTicks) {\n            const x = toCanvasX(value);\n            context.beginPath();\n            context.moveTo(x, axisY);\n            context.lineTo(x, axisY + tickLength);\n            context.stroke();\n        }\n    }\n    context.fillStyle = labelColor;\n    context.textBaseline = 'middle';\n    context.textAlign = 'right';\n    for (const value of yTicks) {\n        const y = toCanvasY(value);\n        const label = yAxis.formatter\n            ? yAxis.formatter(value)\n            : defaultFormatNumber(value);\n        context.fillText(label, plotRect.x - (tickLength + labelOffset), y);\n    }\n    context.textAlign = 'center';\n    context.textBaseline = 'top';\n    for (const value of xTicks) {\n        const x = toCanvasX(value);\n        let label = '';\n        if (xScaleKind === 'category') {\n            label = (_p = categories[Math.round(value)]) !== null && _p !== void 0 ? _p : '';\n            if (xAxis.formatter) {\n                label = xAxis.formatter(label);\n            }\n        }\n        else if (xScaleKind === 'time') {\n            const asDate = new Date(value);\n            label = xAxis.formatter\n                ? xAxis.formatter(asDate)\n                : defaultFormatTime(value);\n        }\n        else {\n            label = xAxis.formatter\n                ? xAxis.formatter(value)\n                : defaultFormatNumber(value);\n        }\n        context.fillText(label, x, plotRect.y + plotRect.height + tickLength + labelOffset);\n    }\n    context.restore();\n}\nfunction drawTitle(context, options, canvasWidth) {\n    var _a, _b, _c, _d;\n    if (!((_a = options.title) === null || _a === void 0 ? void 0 : _a.text)) {\n        return;\n    }\n    context.save();\n    context.fillStyle = (_b = options.title.color) !== null && _b !== void 0 ? _b : '#111827';\n    context.font = (_c = options.title.font) !== null && _c !== void 0 ? _c : 'bold 14px sans-serif';\n    context.textBaseline = 'top';\n    const align = (_d = options.title.align) !== null && _d !== void 0 ? _d : 'left';\n    if (align === 'center') {\n        context.textAlign = 'center';\n        context.fillText(options.title.text, options.position.x + canvasWidth / 2, options.position.y + 2);\n    }\n    else if (align === 'right') {\n        context.textAlign = 'right';\n        context.fillText(options.title.text, options.position.x + canvasWidth, options.position.y + 2);\n    }\n    else {\n        context.textAlign = 'left';\n        context.fillText(options.title.text, options.position.x, options.position.y + 2);\n    }\n    context.restore();\n}\nfunction drawLineSeries(context, series, toCanvasX, toCanvasY) {\n    context.save();\n    context.globalAlpha = series.opacity;\n    context.strokeStyle = series.color;\n    context.lineWidth = series.width;\n    context.beginPath();\n    let hasSegment = false;\n    for (const point of series.data) {\n        const x = toCanvasX(point.x);\n        if (!isFiniteNumber(point.y)) {\n            hasSegment = false;\n            continue;\n        }\n        const y = toCanvasY(point.y);\n        if (!hasSegment) {\n            context.moveTo(x, y);\n            hasSegment = true;\n        }\n        else {\n            context.lineTo(x, y);\n        }\n    }\n    context.stroke();\n    context.restore();\n}\nfunction drawAreaSeries(context, series, yDomain, toCanvasX, toCanvasY) {\n    const baseline = clamp(0, yDomain.min, yDomain.max);\n    const baselineY = toCanvasY(baseline);\n    context.save();\n    context.globalAlpha = series.opacity;\n    context.fillStyle = series.fill;\n    context.strokeStyle = series.color;\n    context.lineWidth = series.width;\n    let segment = [];\n    const flushSegment = () => {\n        if (segment.length < 2) {\n            segment = [];\n            return;\n        }\n        context.beginPath();\n        context.moveTo(segment[0].x, baselineY);\n        for (const point of segment) {\n            context.lineTo(point.x, point.y);\n        }\n        const lastPoint = segment[segment.length - 1];\n        context.lineTo(lastPoint.x, baselineY);\n        context.closePath();\n        context.fill();\n        context.beginPath();\n        context.moveTo(segment[0].x, segment[0].y);\n        for (let i = 1; i < segment.length; i += 1) {\n            context.lineTo(segment[i].x, segment[i].y);\n        }\n        context.stroke();\n        segment = [];\n    };\n    for (const point of series.data) {\n        if (!isFiniteNumber(point.y)) {\n            flushSegment();\n            continue;\n        }\n        segment.push({\n            x: toCanvasX(point.x),\n            y: toCanvasY(point.y),\n        });\n    }\n    flushSegment();\n    context.restore();\n}\nfunction estimateNumericBarWidth(series, toCanvasX) {\n    const xValues = new Set();\n    for (const item of series) {\n        if (item.type !== 'bar' || item.hidden) {\n            continue;\n        }\n        for (const point of item.data) {\n            xValues.add(point.x);\n        }\n    }\n    const sorted = [...xValues].sort((a, b) => a - b);\n    if (sorted.length < 2) {\n        return 24;\n    }\n    let minDelta = Number.POSITIVE_INFINITY;\n    for (let i = 1; i < sorted.length; i += 1) {\n        minDelta = Math.min(minDelta, sorted[i] - sorted[i - 1]);\n    }\n    if (!Number.isFinite(minDelta) || minDelta <= 0) {\n        return 24;\n    }\n    return Math.max(2, Math.abs(toCanvasX(sorted[0] + minDelta) - toCanvasX(sorted[0])) * 0.8);\n}\nfunction drawBarSeries(context, series, allSeries, xScaleKind, categories, plotRect, yDomain, toCanvasX, toCanvasY) {\n    const bars = allSeries.filter(item => item.type === 'bar' && !item.hidden);\n    const barIndex = bars.findIndex(item => item === series);\n    if (barIndex < 0 || bars.length === 0) {\n        return;\n    }\n    let slotWidth = 24;\n    if (xScaleKind === 'category') {\n        slotWidth =\n            categories.length > 0\n                ? (plotRect.width / categories.length) * 0.8\n                : plotRect.width * 0.8;\n    }\n    else {\n        slotWidth = estimateNumericBarWidth(allSeries, toCanvasX);\n    }\n    const width = Math.max(1, slotWidth / bars.length);\n    const offset = -slotWidth / 2 + barIndex * width;\n    const baseline = clamp(0, yDomain.min, yDomain.max);\n    const baselineY = toCanvasY(baseline);\n    context.save();\n    context.globalAlpha = series.opacity;\n    context.fillStyle = series.fill;\n    context.strokeStyle = series.color;\n    context.lineWidth = 1;\n    for (const point of series.data) {\n        if (!isFiniteNumber(point.y)) {\n            continue;\n        }\n        const x = toCanvasX(point.x) + offset;\n        const y = toCanvasY(point.y);\n        const left = x;\n        const top = Math.min(y, baselineY);\n        const height = Math.max(1, Math.abs(y - baselineY));\n        context.beginPath();\n        context.rect(left, top, width, height);\n        context.fill();\n    }\n    context.restore();\n}\nfunction drawScatterSeries(context, series, toCanvasX, toCanvasY) {\n    context.save();\n    context.globalAlpha = series.opacity;\n    context.fillStyle = series.fill;\n    context.strokeStyle = series.color;\n    context.lineWidth = Math.max(1, series.width * 0.5);\n    for (const point of series.data) {\n        if (!isFiniteNumber(point.y)) {\n            continue;\n        }\n        context.beginPath();\n        context.arc(toCanvasX(point.x), toCanvasY(point.y), Math.max(1, series.pointRadius), 0, Math.PI * 2);\n        context.fill();\n        context.stroke();\n    }\n    context.restore();\n}\nfunction drawSeries(context, series, xScaleKind, categories, plotRect, yDomain, toCanvasX, toCanvasY) {\n    for (const item of series) {\n        if (item.hidden) {\n            continue;\n        }\n        if (item.type === 'line') {\n            drawLineSeries(context, item, toCanvasX, toCanvasY);\n        }\n        else if (item.type === 'area') {\n            drawAreaSeries(context, item, yDomain, toCanvasX, toCanvasY);\n        }\n        else if (item.type === 'bar') {\n            drawBarSeries(context, item, series, xScaleKind, categories, plotRect, yDomain, toCanvasX, toCanvasY);\n        }\n        else if (item.type === 'scatter') {\n            drawScatterSeries(context, item, toCanvasX, toCanvasY);\n        }\n    }\n}\nfunction toInputXValue(value, xScaleKind, categories) {\n    if (xScaleKind === 'category') {\n        const label = xToLabel(value);\n        const index = categories.indexOf(label);\n        return index >= 0 ? index : 0;\n    }\n    if (xScaleKind === 'time') {\n        return value instanceof Date ? value.getTime() : Number(value);\n    }\n    return Number(value);\n}\nfunction drawChart(context, options) {\n    const resolved = resolveOptions(options);\n    const normalized = normalizeSeries(resolved.series, resolved.type, resolved.colors);\n    const { canvasWidth, canvasHeight, plotRect } = buildPlotRect(context, resolved);\n    const { xDomain, yDomain } = computeDomains(normalized.series, normalized.xScaleKind, normalized.categories, resolved.xAxis, resolved.yAxis);\n    const scales = makeScales(plotRect, xDomain, yDomain);\n    context.save();\n    if (resolved.background) {\n        context.fillStyle = resolved.background;\n        context.fillRect(resolved.position.x, resolved.position.y, canvasWidth, canvasHeight);\n    }\n    drawAxes(context, plotRect, normalized.xScaleKind, normalized.categories, xDomain, yDomain, resolved.xAxis, resolved.yAxis, resolved.axisStyle, scales.toClampedCanvasX, scales.toClampedCanvasY);\n    drawSeries(context, normalized.series, normalized.xScaleKind, normalized.categories, plotRect, yDomain, scales.toClampedCanvasX, scales.toClampedCanvasY);\n    drawTitle(context, resolved, canvasWidth);\n    context.restore();\n    return {\n        plotRect,\n        xDomain,\n        yDomain,\n        xScaleKind: normalized.xScaleKind,\n        categories: normalized.categories,\n        toCanvasX: (x) => scales.toClampedCanvasX(toInputXValue(x, normalized.xScaleKind, normalized.categories)),\n        toCanvasY: (y) => scales.toClampedCanvasY(y),\n    };\n}\nexports.drawChart = drawChart;\nexports[\"default\"] = drawChart;\n\n\n//# sourceURL=webpack://@basementuniverse/chart/./index.ts?\n}");

/***/ }

/******/ 	});
/************************************************************************/
/******/ 	
/******/ 	// startup
/******/ 	// Load entry module and return exports
/******/ 	// This entry module can't be inlined because the eval devtool is used.
/******/ 	var __webpack_exports__ = {};
/******/ 	__webpack_modules__["./index.ts"](0,__webpack_exports__);
/******/ 	
/******/ 	return __webpack_exports__;
/******/ })()
;
});